# 재발 함정 모음

전부 실제로 당한 사고다. 새로 당했으면 여기 한 절을 추가하라.
(첫 작성: SP 방, 2026-09-02)

## 인코딩·한글

- **HTML 은 `<meta charset="utf-8">` 필수** — 빼면 브라우저에서 mojibake (ì›€ê±°…).
- **.ps1 파일에 한글 금지** — PowerShell 5.1 이 ANSI 로 읽어 깨진다. 한글이 필요한
  생성물은 WSL python 으로 만들 것.
- **한글 파일명은 GitHub API 업로드에서 UnicodeEncodeError** — 릴리즈 자산은 ASCII 만.
- **한글 경로를 PowerShell → 네이티브 exe(tar 등)로 넘기면 코드페이지에서 깨진다** —
  PowerShell cmdlet(Copy-Item -LiteralPath)로 ASCII 경로에 복사한 뒤 exe 를 부를 것.

## WSL·셸

- **/tmp 는 Bash 호출 사이에 증발** (WSL VM 유휴 종료) — 호출을 넘겨 쓸 파일은 ~/ 나 /mnt/c.
- **인라인 셸 변수·루프 변수(`$i`, `$f`)는 도구 경유 호출에서 증발** — 명령은 반드시
  **스크립트 파일로 저장 후** `wsl -e bash <파일>` 로. 히어독·유니코드·백슬래시도 동일.
- **PowerShell 을 거친 히어독은 소리 없이 빈 결과**를 낳기도 한다 — 위와 같은 처방.
- **NGPC 롬셋 7z 는 WSL tar 로 못 연다** — Windows `tar.exe`(bsdtar)로 풀 것.
- **파이썬 `"a" + "b %s" % x` 는 마지막 조각만 포맷된다** (% 가 + 보다 우선) —
  긴 문자열 연결에 % 포맷을 섞으면 조각별로 괄호를 쳐라.

## 빌드·배포

- **apk318.sh 가 versionCode/versionName 을 하드코딩으로 되박는다** — build.gradle 만
  고치면 다음 빌드가 되돌린다. 판올림은 스크립트 쪽 숫자를 고칠 것.
- **ss2-sp-core 의 build/libretro.c 를 고치면 src/ss2sp.patch 를 재생성**해야 한다
  (build.sh 는 트리를 upstream 으로 리셋 후 패치를 다시 입힌다). 재생성은 upstream 을
  얕게 받아 diff → 왕복 적용 검증까지 (regen_patch.sh).
- **빌드 직후 바로 회귀를 돌리면 낡은 .so 를 잡는다** — `sleep 2` (세 번 당함).
- **릴리즈 자산을 크기로 비교해 교체를 건너뛰지 마라** — 번역만 바뀐 같은 크기 판을
  「같은 파일」로 넘긴 전력. 늘 지우고 다시 올려라.
- **배포 후 내용이 바뀌면 판번호를 올려라** — 같은 이름 덮어쓰기는 받은 사람과
  본문 해시가 어긋난다 (실제 제보).

- **하네스가 코어 옵션 기본값을 다 안 주면 화면 크기가 조용히 바뀐다.** RetroArch 없이
  코어를 직접 로드하는 하네스(`ngprun`·`svcrun`)는 옵션을 스스로 먹여야 하는데,
  **빠뜨린 옵션의 처리가 코어마다 다르다.** 대부분은 `if (조회성공 && var.value)` 라
  안 주면 무해한데 지오메트리 옵션 둘이 예외다 —
  `ngp_svcsp_band` 는 조회 실패 시 `int on = 1` 로 **켠다**(높이 152→184),
  `ngp_ss2sp_sides` 는 코어 초기값이 `true` 라 안 주면 켜진 채다(폭 160→288).
  그래서 순정 코어는 `160x152`, SP 코어는 `288x184` 를 뱉고 **PPM 바이트 비교가
  조용히 어긋난다.** 크기가 다른 걸 보고도 「디버그 빌드라 그런가」 하고 지나간 전과.
  → 하네스 DEFAULTS 는 **코어가 실제로 읽는 키 전체**와 대조해서 채워라:
  `grep -o '"ngp_[a-z0-9_]*"' libretro.c | sort -u` 로 뽑아 빠진 것을 찾는다.
  (`ngprun` 은 8개만 주고 있었고 그중 하나는 코어가 읽지도 않는 죽은 키였다.)

## 안드로이드

- **sdcard 는 noexec — dlopen 이 안 되는 기기가 많다.** 내려받은 코어(.so)는 앱 내부
  저장소(getFilesDir)에 두고 로드할 것.
- **AAudio 는 에러 콜백 없이는 장치를 뺏기면 무음으로 죽는다** — setErrorCallback +
  재생성, onPause 에서 스트림 반납이 정석.
- **큰 파일(수백 MB 팩)을 ByteArrayOutputStream 으로 받으면 OOM** — 스트리밍으로 파일에.
- **다운로드 파일은 매직 검사** (IPS=`PATCH`, ELF=`7f 45 4c 46`) — 오류 페이지를
  패치/코어로 저장하는 사고 방지.
- 옆설치 서명은 고정 키스토어(잃어버리면 유저 전원 재설치) — 키 이름·자리는 SP 방 관리.

## 실측·검수 (요약 — 상세는 methodology.md)

- 결과 해시는 원본 덤프마다 다르다 ([!]/[a1] 세이브 플래시) — 「어느 덤프든 같은 결과」금지.
- 검증은 반드시 깨끗한 원본으로 — 작업 폴더의 롬은 이미 패치본인 경우가 많다.
