# 인계서 — 네오지오 포켓 한글패치 방

2026-09-06 · 본부(1-e8) 작성. **새 방이 이 문서 하나로 일어설 수 있게** 썼다.

---

## 0. 먼저 읽을 다섯 줄

1. **롬은 배포하지 않는다.** IPS 차분만 낸다. 순정 롬은 `~/ss2/rom/pristine/` 에만 둔다.
2. **판 번호는 유저가 정한다.** 방이 스스로 정식 판을 올리지 않는다.
3. **측정하지 않은 것은 말하지 않는다.** 표·파일 이름·남의 JSON 은 증거가 아니다. 롬 바이트가 증거다.
4. **잰 것은 «무엇을 쟀는지»(파일 이름 + md5)를 같이 적는다.** 이것만 지켜도 오늘 겪은 사고 절반이 안 난다.
5. **방 공유 지식은 `~/ss2/repo/thinkbox/knowledge/traps.md`** 에 있다. **세션 시작하면 일독한다.**

---

## 1. 방 구성

| 방 | 맡은 것 |
|---|---|
| **본부 (1-e8)** | PocketCore 앱 · 배포 전부(KrPatch·InputPatch·PocketCore) · 검산 · 방 조율 · 기억 |
| **이식소 (1-df)** | 화면 실측 · VRAM · 에뮬 하니스 · 프레임 계측 |
| **대사엔진 (1-f7)** | SS2 대사·글리프 엔진 · 그림 아이템 삽입 |
| **한패공방 (claude-eb)** | 카드 파이터즈 1·2 · 월화의 검사 |

**본부의 원칙**: 다른 방이 낸 값은 **받아서 다시 잰다.** 오늘 그 습관이 네 번 사고를 잡았다.
**권한은 방마다 따로다.** 다른 방에 「내 권한으로 막힌 일」을 시키지 않는다.

---

## 2. 게임별 상태

### 사무라이 쇼다운! 2 (SS2)
- 대상 롬 `Samurai Shodown! 2 (JUE) [!].ngc` md5 `8d6a36acd3b171970d87d2fca6796a4a`
- **정식 v1.0** · **시험 v1.2** (붓글씨 배너 여섯: 승부·일/이/삼회전·자아·당당히)
- 작업 폴더 `~/ss2/work_lang/v10/`
- **지금 진행 중**: 화면 안 «그림 글자» 48개 외주 발주. 아래 §5 참조.
- 남은 것: 수라나찰(팔레트 대응 미확정) · 完勝 · 一本 · 로고 재작업

**★ 절대 건드리지 말 것**
- **저장 영역 `0x1F0200~`** — 기본판은 한 바이트도 안 건드린다(올카드판만 72 B)
- **카드 제목 색인표 `0x0822D2`** (u16×120) — 기록 시작 주소와 어긋나면 **어긋난 첫 기록 번호 = 첫 깨지는 카드 번호**. 카드 114장을 깨뜨린 적이 있다.
- **카드 설명 글은 옮기지도, 길이를 바꾸지도 않는다** (`0x0319EB` 표 → `0x031ADB~0x033ED7`)

**관문**: `~/ss2/repo/thinkbox/tools/ss2_release_gate.py <롬>` — 아홉 항. 배포 전 반드시 통과.

### 월화의 검사 특별편
- 대상 롬 `Last Blade, The (UE) [!].ngc` md5 `83fa4cb3c629aa57c12f9cec52c5a53d`
- **정식 v0.24** · **시험 v0.26** (그림 글자 넷: 준비·라운드·베어라!·승리!)
- 작업 폴더 `C:\Claude\월화 한글\` · 도구 `tools/gfx_put.py`(자산 자리표) · `gfx_check.py`(검사기)
- **★ 승리! 자리**: `0x067808~0x067AA7` · 8×6칸 · 42칸. 베어라! `0x067618~` → 승리! → 훌륭! `0x067AAC~` **셋이 이어져 있다.**
- **★ 빈 타일 `0x000030` 을 여러 자산이 나눠 쓴다.** 거기 쓰면 다른 자산까지 망가진다.
- 남은 것: 훌륭!(원화 없음) · 옵션 머리띠 · 힘/력 게이지. **PRESS A BUTTON 과 이름판은 글꼴이라 그림 발주 대상이 아니다.**

### 카드 파이터즈 클래시 1·2
- CFC1 UE **v0.12** 배포됨 / CFC2 J **v0.1** 배포됨, **v0.2 검수 중**
- 순정 CFC2 `SNK Vs Capcom - Card Fighters Clash 2 - Expand Edition (J) [!].ngc` md5 `ebc0e8c33d1a5e5be5457ad988233feb`
- **지금 막힌 곳**: 바깥 검수자에게 두 번 반려됐다. 아래 §6 참조.

### KOF R-2 · SvC
- R-2 한패 **v0.2.1** · **`0x1E0000~` 을 건드리지 않는다**(소리를 죽인 사고가 있었다)
- SvC 한패 v17.2 계열 · 인풋 패치는 **InputPatch** 레포

---

## 3. 배포 구조

| 레포 | 무엇 |
|---|---|
| `rmdkdkr-png/KrPatch` | **한글 패치**(IPS). 태그는 게임별 — `v1.2`(SS2) · `lastblade-v0.26` 꼴 |
| `rmdkdkr-png/InputPatch` | **조작 패치**. `mods` 태그 |
| `rmdkdkr-png/PocketCore` | **앱**과 색인. `app` 태그에 `patches.json` · `cores.json` · APK |

**갈래가 둘이다.**
- **정식** — 보통 릴리즈. 앱이 모두에게 내려준다.
- **시험** — **prerelease 딱지**. 「최신」에 안 잡히고 배포 스크립트가 건너뛴다.
  앱 `options.txt` 에 `pocketcore_level=test` 인 기기만 받는다. `patches.json` 의 `test` 칸이 그것.

**★ `patches.json` 을 고칠 때**: **캐시를 우회해 읽고**(질의에 시각 붙이기 + `Cache-Control: no-cache`), **쓴 뒤 다시 읽어 «안 고친 항목»까지 확인한다.** 캐시본을 읽어 고쳐 쓰다가 남의 방금 고침을 지운 적이 있다.

**앱 빌드·서명**: `~/ss2/release/tools/apk318.sh` (키스토어 `~/ss2/release/dudu.jks`).
**★ 이 스크립트에는 비밀번호가 들어 있다 — 공개 레포에 절대 넣지 않는다.** `~/ss2/release` 는 git 밖이다.
**서명 안 한 APK 를 유저에게 주지 않는다.** 앱은 «업데이트 확인»으로 받는 것이 정상 경로다.

---

## 4. 그림 글자를 다루는 법 (NGP·NGPC 공통)

큰 글자는 글꼴이 아니라 **그림 아이템**이다: 타일 목록 + 칸마다 (타일, 팔레트) 지도.

    cnt16 | ptr32 | off16 × cnt | 헤더 1B | 행 × [ 칸 × (타일, 팔레트바이트) ] + ff ff

- 파일 오프셋 = CPU 주소 − 0x200000
- 타일 16 B, 2bpp. 픽셀 값 = 워드 비트(15−2x, 14−2x), **MSB 먼저**. **평면(planar)이 아니라 묶음(packed)이다** — `55 55` 는 한 색 여덟 화소다.
- **8×8 칸 하나에 팔레트 하나** → 칸마다 잉크 세 가지까지(투명 빼고)
- **행마다 첫 칸은 «암묵»이다** — 기록에 없고 빈 타일로 나온다. 곧 **맨 왼쪽 세로줄을 못 쓴다.**
- 예산: **칸 수 ≤ 타일 색인 한도 256 ≤ 뱅크 자리**(형제 아이템과 나눠 쓴다)
- 도구: `thinkbox/tools/logo_tool.py` + 게임별 프로파일(`profile_ss2.json` 등)

**★ 그림을 판정하는 잣대 다섯** (오늘 세 게임에서 굳은 것)
1. 크기는 **캔버스가 아니라 잉크 네모**로 견준다
2. 색은 **값이 아니라 밝기 순위**로 역할을 정한다 (`#410000` 과 `65,0,0` 이 같다는 보증은 없다)
3. **어두운 잉크는 외곽선 전용이 아니다** — 면에 깔리는 그늘 디더인 경우가 많다
4. **색마다 화소 수를 원문의 90~110%** 로 (종류가 맞다는 것과 양이 맞다는 것은 다른 검사다)
5. **원문 잉크가 0인 칸에는 그려도 안 나온다**
6. **바탕이 투명이 아닌 자산이 있다** — 칸 화소를 다 세면 잉크가 부풀어 오른다

---

## 5. 지금 진행 중 — SS2 그림 글자 발주

- 롬 안 **그림 아이템 표 `0x06F56D`** (u16×48). **이것이 전수 목록이다** — 화면을 훑을 필요가 없다.
- 48개 중 **한글 들어감 39 · 원문 그대로 9**(카운트다운 숫자 — 그대로 두는 게 맞다)
- **원문의 80% 미만이 23개** — 이것이 발주 대상. 나쁜 순서로 승부결정 27% · 표36 33% · 재대결할까? 40%
- **★ 회전 숫자가 一~十 열 개인데 일·이·삼만 고쳐져 있다.** 넷째 판부터 한자로 뜬다.
- 묶음: `~/ss2/외주세트/사쇼2_그림글자/` (발주문 · 규격표 · 그림 192장 · 칸마스크 48장)

**외주가 짚어 아직 못 준 것 넷** (이식소에 요청해 둠)
1. ~~칸 위치 마스크~~ → 만들었다
2. **표16 의 팔레트** — 「진회색 3,942화소가 획인지 바탕인지」. **내 «가장 많은 색인 = 바탕» 어림이 틀렸을 수 있다.**
3. **「승부」 조각 열둘(13·14·38~47)의 조립 좌표** — 따로 고치면 이어 붙였을 때 획이 안 맞는다
4. **표16 실제 화면 사진**

**아직 이름을 못 붙인 표**: 15·19·20·21·22·23·25·36·37 (한글은 이미 들어가 있다)

---

## 6. 지금 막힌 곳 — CFC2 검수

바깥 검수자에게 **두 번 반려**됐다. 사유가 배울 만하다.

1. **1차·2차**: 자리표시자 뒤 조사 공백 73곳 등 → 고쳤다
2. **3차**: IPS 는 새것인데 **검수 문서와 그림이 구판** → 문서는 다시 뽑았다
3. **핵심 지적**: 「**제작자의 자체 검산은 독립 검산 증거가 아니다**」

**본부가 독립 검산해서 나온 것**
- 지적 여섯은 **롬 바이트로 전부 고쳐진 것 확인**
- ★ **`0x0FB5A4` — 631바이트 일본어 대사가 «살아서» 남아 있다.** 포인터 3,174칸 중 «순정과 같은 곳을 가리키고 바이트도 같은» 칸이 538개, 그중 4바이트 이상은 **정확히 이 하나**. 「백업 100% 달성」 보상 대사다.
- **그리고 검수 문서가 빠뜨린 유일한 항목이 정확히 이것이다.** 번역 구멍과 문서 구멍이 같은 자리였다.
- ★ **구조적 결함**: `cf2_review.py` 가 **«순정 롬 + 빌드 재료»** 로 문서를 찍는다. **구운 롬을 안 읽는다.** 그래서 문서가 맞아도 롬이 맞다는 증거가 안 된다. **패치본 롬 하나만 받는 추출기**가 있어야 한다 — 본부가 지은 것이 `scratchpad/cfc2_extract_indep.py`.

---

## 7. 오늘 배운 함정 (traps.md ㉚~㉝)

- **㉚ 무늬로 싹 지우면 맞는 것까지 지운다.** 「{1x}+공백」 175곳 중 **99곳은 띄어야 맞았다.** 일괄 치환은 «무엇을 안 바꾸나»를 세어 확인한다.
- **㉛ 가짜 ✔ — 검사기가 참고할 것을 못 찾으면 조용히 통과시킨다.** ✗ 는 사람을 붙잡지만 **✔ 는 그냥 지나간다.** 검사기는 **알려진 성한 것과 알려진 망가진 것 둘 다**에 돌려 본다.
- **㉛-3 대조군이 «덮여» 있으면 0 차이가 나오고 그것을 합격으로 읽는다.** 대조기는 두 입력이 같으면 «멈춤»을 내야 한다.
- **㉜ 색 «값»이 아니라 «순위»로 역할을 정하라.** 바깥 원화가 두 규약을 섞어 쓴 적이 있다.
- **㉝ 캐시된 것을 읽어 고쳐 쓰면 남의 방금 고침이 되돌아간다.**

---

## 8. 자리

```
~/ss2/rom/pristine/            순정 롬 (대조군은 여기서만)
~/ss2/repo/thinkbox/           방 공유 지식·도구  ★ knowledge/traps.md 먼저
~/ss2/repo/KrPatch             한글패치 배포 레포
~/ss2/repo/pocketcore/pocketcore/   ★ 앱 «진짜» 빌드 트리 (local.properties 있는 쪽)
~/ss2/release/tools/           APK 빌드·서명 (git 밖 — 비밀번호 들어 있음)
~/ss2/work_lang/v10/           SS2 작업
~/ss2/외주세트/                 외주 발주 묶음
C:\Claude\월화 한글\            월화 작업
C:\Claude\카드파이터즈 한글\      CFC 작업
```

**셸 함정**: `wsl -e bash -lc "…"` 안에 홑따옴표·역따옴표를 넣으면 바깥 셸이 먹어서 파일이 망가진다.
**파이썬은 파일로 써서 경로로 실행한다.** 히어독도 피한다.
